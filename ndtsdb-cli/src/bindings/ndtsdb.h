#ifndef NDTSDB_BINDINGS_H
#define NDTSDB_BINDINGS_H

// 直接包含 native 头文件，避免类型重复定义
// Makefile 包含 -I../ndtsdb/native，所以使用相对路径
#include "../../../ndtsdb-lib/native/ndtsdb.h"

#endif // NDTSDB_BINDINGS_H
